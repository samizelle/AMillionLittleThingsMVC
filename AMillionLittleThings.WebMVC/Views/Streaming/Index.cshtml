@model IEnumerable<AMillionLittleThings.Models.StreamingListItem>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.StreamingId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.StreamingTitle)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.StreamingNote)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CreatedUtc)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.StreamingId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.StreamingTitle)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.StreamingNote)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CreatedUtc)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
